import React from "react";

import { HeaderProps } from "./Header.interface.ts";
import styles from "./Header.module.css";

const Header: React.FC<HeaderProps> = ({ }) => {
	return (
		Header
	);
};

export default Header;
